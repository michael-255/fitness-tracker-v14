import { describe, test, expect } from 'vitest'
import { _Record } from '../_Record'

describe('_Record', () => {
  test('should test _Record', () => {
    expect(new _Record()).toBe(false)
  })

  test('should test getParentEntity method', () => {
    expect(true).toBe(false)
  })
})
